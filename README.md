# newDocker
script to build templated docker-compose files


Usage: ./newDocker [service_name]

The service name is optional.  If used from within a directory, we'll assume the service name is the cwd.


